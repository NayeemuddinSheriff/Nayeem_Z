/*
Do the following.
Create a function to sum two numbers and return the value. 
Create a function to generate a random number between 0 to 10
Pass two values to the sum function, But first value should be passed by the user and the second value should be generated by a random number
*/

<!DOCTYPE html>
<html>
<title>Web Page Design</title>
<head>
<script>

var user = prompt("Enter the First Integer");
user = parseInt(user);
var random;
var a;

function myfunc(){
    random = Math.floor(Math.random() * 11)
}
myfunc();

function sum(x,y){
    console.log(x,y);   //Checking whether x and y are correct values
    a = x + y;
    console.log(a)
}
console.log(typeof(user))
sum(user,random);

</script>
</head>
<body>
</body>
</html>
